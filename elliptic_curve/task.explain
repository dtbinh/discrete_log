В файле task.txt данные следуют в таком порядке:
A B - коэффициенты эллиптической кривой y^2=x^3+A*x+B
p - модуль поля Галуа GF(p), над которым определена кривая
Q - элемент, образующий циклическую группу (два числа x y)
r - порядок циклической группы, образованной Q
P - точка эллиптической кривой, P=Q*l
